<UserControl x:Class="Interface.Export.ExportPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:res="clr-namespace:Interface.Resources"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Label Grid.Column="0" Grid.Row="0" Content="{x:Static res:Lang.Export_Amount}" Padding="0" Margin="0, 0, 3, 0"/>
            <TextBox Grid.Column="1" Grid.Row="0" Name="AmountBox"/>
            <!-- <Label Grid.Column="0" Grid.Row="1" Content="{x:Static res:Lang.Export_AnswerCount}" Padding="0" Margin="0, 3, 3, 0"/> -->
            <!-- <TextBox Grid.Column="1" Grid.Row="1" Name="AnswerCountBox"  Margin="0, 3, 0, 0"/> -->
            <ComboBox Grid.Column="1" Grid.Row="2" Padding="3, 0" Margin="0, 3, 0, 0">
                <ComboBoxItem Content="{x:Static res:Lang.Export_ShuffleSmart}" Selected="SmartShuffle_OnSelected" IsSelected="True"/>
                <ComboBoxItem Content="{x:Static res:Lang.Export_Shuffle}" Selected="ShuffleOrder_OnSelected"/>
                <ComboBoxItem Content="{x:Static res:Lang.Export_Ordered}" Selected="KeepOrder_OnSelected"/>
            </ComboBox>
            <Label Grid.Column="0" Grid.Row="3" Content="{x:Static res:Lang.Export_Title}" Padding="0" Margin="0, 3, 3, 0"/>
            <TextBox Grid.Column="1" Grid.Row="3" Margin="0, 3, 0, 0" Name="TitleBox"/>
            <Label Grid.Column="0" Grid.Row="4" Content="{x:Static res:Lang.Export_Author}" Padding="0" Margin="0, 3, 3, 0"/>
            <TextBox Grid.Column="1" Grid.Row="4" Margin="0, 3, 0, 0" Name="AuthorBox"/>
            <Label Grid.Column="0" Grid.Row="5" Content="{x:Static res:Lang.Export_Date}" Padding="0" Margin="0, 3, 3, 0"/>
            <TextBox Grid.Column="1" Grid.Row="5" Margin="0, 3, 0, 0" Name="DateBox"/>
            <Label Grid.Column="0" Grid.Row="6" Content="{x:Static res:Lang.Export_Path}" Padding="0" Margin="0, 3, 3, 0"/>
            <Grid Grid.Column="1" Grid.Row="6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Margin="0, 3, 0, 0" Name="ExportBox"/>
                <Button Grid.Column="1" Margin="3, 3, 0, 0" Content="{x:Static res:Lang.Export_Browse}" Padding="3, 0" Click="BrowseButton_OnClick"/> <!-- Localize -->
            </Grid>
            <Button Grid.Column="0" Grid.Row="7" Grid.ColumnSpan="2" Content="{x:Static res:Lang.Export_Export}" Margin="0, 3, 0, 0" Click="ExportButton_OnClick"/>
            <Label Grid.Column="0" Grid.Row="8" Grid.ColumnSpan="2" Content="{x:Static res:Lang.Export_Exceptions}" Padding="0" Margin="0, 3, 3, 0"/>
            <TextBox Grid.Column="0" Grid.Row="9" Grid.ColumnSpan="2" Margin="0, 3, 0, 0" AcceptsReturn="True" Name="ExceptionList" TextChanged="ExceptionList_OnTextChanged"/>
        </Grid>
        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Width="5" Margin="3,0,3,0" />
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" BorderBrush="{DynamicResource {x:Static SystemColors.ScrollBarBrushKey}}" BorderThickness="1">
                <ScrollViewer Background="{DynamicResource {x:Static SystemColors.MenuBrushKey}}"
                              HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <Canvas Name="PreviewScroll"/>
                </ScrollViewer>
            </Border>
            <ProgressBar Grid.Row="1" Height="10" IsIndeterminate="True" Name="PreviewProgressBar" Visibility="Collapsed" Margin="0,3,0,0"/>
            <Grid Grid.Row="2" HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Name="CreateButton" Content="{x:Static res:Lang.Export_Preview}" Click="ButtonBase_OnClick" Margin="0, 3, 3, 0" Padding="3, 0"/>
                <Button Grid.Column="1" Content="{x:Static res:Lang.Export_ZoomOut}" Click="ZoomOut_OnClick" Margin="0, 3, 3, 0" Padding="3, 0"/>
                <Button Grid.Column="2" Content="{x:Static res:Lang.Export_ZoomIn}" Click="ZoomIn_OnClick" Margin="0, 3, 3, 0" Padding="3, 0"/>
                <Label Grid.Column="3" HorizontalAlignment="Right" Margin="3, 3, 0, 0" Padding="3, 0" Content="{x:Static res:Lang.Export_Resolution}"/>
                <ComboBox Grid.Column="4" Margin="3, 3, 0, 0" Padding="3, 0">
                    <ComboBoxItem Content="{x:Static res:Lang.Export_High}" Selected="ScaleLarge_OnSelect"/>
                    <ComboBoxItem Content="{x:Static res:Lang.Export_Med}" IsSelected="True" Selected="ScaleMedium_OnSelect"/>
                    <ComboBoxItem Content="{x:Static res:Lang.Export_Low}" Selected="ScaleSmall_OnSelect"/>
                </ComboBox>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
